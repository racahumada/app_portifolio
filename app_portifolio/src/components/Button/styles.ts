import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 150,
    borderRadius: 8,
    backgroundColor: '#FAC898',
    paddingVertical: 8,
    alignItems: 'center',
  },
  text: {
    color: '#555',
    fontSize: 14,
  },
});
