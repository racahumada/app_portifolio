import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    gap: 20,
    backgroundColor: '#FFF5EE',
  },
  image: {
    width: 200,
    height: 200,
    borderRadius: 100,
  },
  nameText: {
    fontSize: 18,
    fontWeight: 500,
  },
  containerLinks: {
    gap: 8,
    alignItems: 'center',
  },
  containerButtons: {
    gap: 5,
  },
  titleLinks: {
    fontSize: 14,
    fontWeight: 600,
  },
});
